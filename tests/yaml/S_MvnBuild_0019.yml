template: true
valuesFilePath: ../values/gitRepoValues.yml

resources:
  - name: S_MvnBuild_0019_GitRepo
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: {{ .Values.GitHub.Org_test_Automation_bash.path }}
      branches:
        include: {{gitBranch}}

pipelines:
  - name: pipeline_S_MvnBuild_0019
    steps:
      - name: S_MvnBuild_0019
        type: MvnBuild
        configuration:
          inputResources:
            - name: S_MvnBuild_0019_GitRepo
          integrations:
            - name: s_artifactory
          configFileName: configName
          configFileLocation: configLocation
          resolverSnapshotRepo: resolver-snapshot
          resolverReleaseRepo: resolver-release
