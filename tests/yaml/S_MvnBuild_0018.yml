template: true
valuesFilePath: ../values/gitRepoValues.yml

resources:
  - name: S_MvnBuild_0018_GitRepo
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: {{ .Values.GitHub.Org_test_Automation_bash.path }}
      branches:
        include: {{gitBranch}}

pipelines:
  - name: pipeline_S_MvnBuild_0018
    steps:
      - name: S_MvnBuild_0018
        type: MvnBuild
        configuration:
          inputResources:
            - name: S_MvnBuild_0018_GitRepo
          integrations:
            - name: s_artifactory
          deployerReleaseRepo: deployer-release
