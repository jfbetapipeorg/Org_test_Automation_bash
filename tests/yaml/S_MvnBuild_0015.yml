template: true
valuesFilePath: ../values/gitRepoValues.yml

resources:
  - name: S_MvnBuild_0015_GitRepo
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: {{ .Values.GitHub.Org_test_Automation_bash.path }}
      branches:
        include: {{gitBranch}}

pipelines:
  - name: pipeline_S_MvnBuild_0015
    steps:
      - name: S_MvnBuild_0015
        type: MvnBuild
        configuration:
          inputResources:
            - name: S_MvnBuild_0015_GitRepo
          integrations:
            - name: s_artifactory
          resolverReleaseRepo: resolver-release
