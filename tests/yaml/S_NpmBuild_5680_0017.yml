template: true
valuesFilePath: ../values/gitRepoValues.yml

resources:
  - name: S_NpmBuild_5680_0017_GitRepo
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: {{ .Values.GitHub.Org_test_Automation_bash.path }}
      branches:
        include: {{gitBranch}}

pipelines:
  - name: pipeline_S_NpmBuild_5680_0017
    configuration:
      environmentVariables:
        readOnly:
          repo_name: test-automation-npm-remote
    steps:
      - name: S_NpmBuild_5680_0017
        type: NpmBuild
        configuration:
          inputResources:
            - name: S_NpmBuild_5680_0017_GitRepo
          integrations:
            - name: s_artifactory
          sourceLocation: "tests/npm"
          repositoryName: "${repo_name}"
